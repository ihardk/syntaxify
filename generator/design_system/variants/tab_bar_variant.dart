/// TabBar variant enum
///
/// Defines visual variants for the tab bar component:
/// - primary: Primary style with filled indicator
/// - secondary: Secondary style with minimal styling
enum TabBarVariant {
  primary,
  secondary,
}
