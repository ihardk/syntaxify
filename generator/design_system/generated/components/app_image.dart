// ============================================
// GENERATED BY SYNTAXIFY v0.2.0-beta
// DO NOT MODIFY - Regenerated on build
// Component: AppImage (Meta-Driven)
// Generated: 2025-12-27
// ============================================

import 'package:flutter/material.dart';
import '../../design_system/design_system.dart';

/// A design-system-aware Image component.
///
/// This widget delegates rendering to [DesignStyle.renderImage].
/// Properties are defined in the corresponding .meta.dart file.
class AppImage extends StatelessWidget {
  const AppImage({
    super.key,
    required this.src,
    this.width,
    this.height,
    this.fit,
    this.placeholder,
    this.errorWidget,
    this.borderRadius = 0.0,
  });

  /// Creates a square image.
  const AppImage.square({
    super.key,
    required this.src,
    required double size,
    this.fit,
    this.placeholder,
    this.errorWidget,
    this.borderRadius = 0.0,
  })  : width = size,
        height = size;

  /// Creates a circular avatar image.
  const AppImage.avatar({
    super.key,
    required this.src,
    required double size,
    this.placeholder,
    this.errorWidget,
  })  : width = size,
        height = size,
        fit = BoxFit.cover,
        borderRadius = size / 2;

  /// Image source (asset path or network URL)
  final String src;

  /// Image width in logical pixels
  final double? width;

  /// Image height in logical pixels
  final double? height;

  /// How the image should fit within its bounds
  final BoxFit? fit;

  /// Placeholder widget while loading (network images)
  final Widget? placeholder;

  /// Error widget if image fails to load
  final Widget? errorWidget;

  /// Border radius for rounded corners
  final double borderRadius;

  @override
  Widget build(BuildContext context) {
    return AppTheme.of(context).style.renderImage(
      src: src,
      width: width,
      height: height,
      fit: fit,
      placeholder: placeholder,
      errorWidget: errorWidget,
      borderRadius: borderRadius,
    );
  }
}
