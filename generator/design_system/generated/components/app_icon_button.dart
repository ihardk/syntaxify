// ============================================
// GENERATED BY SYNTAXIFY v0.2.0-beta
// DO NOT MODIFY - Regenerated on build
// Component: AppIconButton (Meta-Driven)
// Generated: 2025-12-27
// ============================================

import 'package:flutter/material.dart';
import '../../design_system.dart';

/// A design-system-aware IconButton component.
///
/// This widget delegates rendering to [DesignStyle.renderIconButton].
/// Properties are defined in the corresponding .meta.dart file.
class AppIconButton extends StatelessWidget {
  const AppIconButton({
    super.key,
    required this.icon,
    this.onPressed,
    this.variant = IconButtonVariant.standard,
    this.size = 24.0,
    this.color,
    this.isDisabled = false,
    this.tooltip,
  });

  /// Creates a filled icon button.
  const AppIconButton.filled({
    super.key,
    required this.icon,
    this.onPressed,
    this.size = 24.0,
    this.color,
    this.isDisabled = false,
    this.tooltip,
  }) : variant = IconButtonVariant.filled;

  /// Creates an outlined icon button.
  const AppIconButton.outlined({
    super.key,
    required this.icon,
    this.onPressed,
    this.size = 24.0,
    this.color,
    this.isDisabled = false,
    this.tooltip,
  }) : variant = IconButtonVariant.outlined;

  /// Icon name from AppIcons
  final String icon;

  /// Callback when button is pressed
  final VoidCallback? onPressed;

  /// Button variant
  final IconButtonVariant variant;

  /// Icon size
  final double size;

  /// Icon/button color (overrides default)
  final Color? color;

  /// Whether the button is disabled
  final bool isDisabled;

  /// Tooltip text
  final String? tooltip;

  @override
  Widget build(BuildContext context) {
    return AppTheme.of(context).style.renderIconButton(
      icon: icon,
      onPressed: onPressed,
      variant: variant,
      size: size,
      color: color,
      isDisabled: isDisabled,
      tooltip: tooltip,
    );
  }
}
