// ============================================
// GENERATED BY SYNTAXIFY v0.2.0-beta
// DO NOT MODIFY - Regenerated on build
// Component: AppTabBar (Meta-Driven)
// Generated: 2025-12-27
// ============================================

import 'package:flutter/material.dart';
import '../../design_system.dart';

/// A design-system-aware tab bar component for navigation.
///
/// This widget delegates rendering to [DesignStyle.renderTabBar].
/// Properties are defined in the corresponding .meta.dart file.
class AppTabBar extends StatelessWidget {
  const AppTabBar({
    super.key,
    required this.tabs,
    required this.currentIndex,
    required this.onTabChange,
    this.variant = TabBarVariant.primary,
    this.isScrollable = false,
  });

  /// Creates a secondary tab bar.
  const AppTabBar.secondary({
    super.key,
    required this.tabs,
    required this.currentIndex,
    required this.onTabChange,
    this.isScrollable = false,
  }) : variant = TabBarVariant.secondary;

  /// List of tab items (label and optional icon)
  final List<TabBarItem> tabs;

  /// Currently selected tab index
  final int currentIndex;

  /// Callback when tab changes
  final ValueChanged<int> onTabChange;

  /// Tab bar variant
  final TabBarVariant variant;

  /// Whether tabs are scrollable
  final bool isScrollable;

  @override
  Widget build(BuildContext context) {
    return AppTheme.of(context).style.renderTabBar(
      tabs: tabs,
      currentIndex: currentIndex,
      onTabChange: onTabChange,
      variant: variant,
      isScrollable: isScrollable,
    );
  }
}
