// ============================================
// GENERATED BY SYNTAXIFY v0.2.0-beta
// DO NOT MODIFY - Regenerated on build
// Component: AppDropdown (Meta-Driven)
// Generated: 2025-12-27
// ============================================

import 'package:flutter/material.dart';
import '../../design_system.dart';

/// A design-system-aware dropdown/select component.
///
/// This widget delegates rendering to [DesignStyle.renderDropdown].
/// Properties are defined in the corresponding .meta.dart file.
class AppDropdown<T> extends StatelessWidget {
  const AppDropdown({
    super.key,
    required this.value,
    required this.items,
    required this.onChanged,
    this.variant = DropdownVariant.standard,
    this.label,
    this.hint,
    this.enabled = true,
    this.errorText,
  });

  /// Creates an outlined dropdown.
  const AppDropdown.outlined({
    super.key,
    required this.value,
    required this.items,
    required this.onChanged,
    this.label,
    this.hint,
    this.enabled = true,
    this.errorText,
  }) : variant = DropdownVariant.outlined;

  /// Creates an underlined dropdown.
  const AppDropdown.underlined({
    super.key,
    required this.value,
    required this.items,
    required this.onChanged,
    this.label,
    this.hint,
    this.enabled = true,
    this.errorText,
  }) : variant = DropdownVariant.underlined;

  /// Currently selected value
  final T? value;

  /// List of dropdown items (value, label pairs)
  final List<DropdownItem<T>> items;

  /// Callback when value changes
  final ValueChanged<T?>? onChanged;

  /// Dropdown variant
  final DropdownVariant variant;

  /// Label text above dropdown
  final String? label;

  /// Hint text when no value selected
  final String? hint;

  /// Whether dropdown is enabled
  final bool enabled;

  /// Error text to display
  final String? errorText;

  @override
  Widget build(BuildContext context) {
    return AppTheme.of(context).style.renderDropdown<T>(
      value: value,
      items: items,
      onChanged: onChanged,
      variant: variant,
      label: label,
      hint: hint,
      enabled: enabled,
      errorText: errorText,
    );
  }
}
