// ============================================
// GENERATED BY SYNTAXIFY v0.2.0-beta
// DO NOT MODIFY - Regenerated on build
// Component: AppIcon (Meta-Driven)
// Generated: 2025-12-27
// ============================================

import 'package:flutter/material.dart';
import '../../design_system/design_system.dart';

/// A design-system-aware Icon component.
///
/// This widget delegates rendering to [DesignStyle.renderIcon].
/// Properties are defined in the corresponding .meta.dart file.
class AppIcon extends StatelessWidget {
  const AppIcon({
    super.key,
    required this.name,
    this.size,
    this.color,
    this.semanticLabel,
  });

  /// Creates a small icon.
  const AppIcon.small({
    super.key,
    required this.name,
    this.color,
    this.semanticLabel,
  }) : size = 16.0;

  /// Creates a medium icon (default).
  const AppIcon.medium({
    super.key,
    required this.name,
    this.color,
    this.semanticLabel,
  }) : size = 24.0;

  /// Creates a large icon.
  const AppIcon.large({
    super.key,
    required this.name,
    this.color,
    this.semanticLabel,
  }) : size = 32.0;

  /// The icon name from AppIcons
  final String name;

  /// Icon size in logical pixels
  final double? size;

  /// Icon color (overrides default)
  final Color? color;

  /// Semantic label for accessibility
  final String? semanticLabel;

  @override
  Widget build(BuildContext context) {
    return AppTheme.of(context).style.renderIcon(
      name: name,
      size: size,
      color: color,
      semanticLabel: semanticLabel,
    );
  }
}
