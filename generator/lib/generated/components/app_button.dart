// ============================================
// GENERATED BY FORGE v0.1.0
// DO NOT MODIFY - Regenerated on build
// Source: meta/button.meta.dart
// Generated: 2025-12-17T11:30:29.030972
// ============================================

import 'package:flutter/material.dart';
import '../theme/app_theme.dart';

class AppButton extends StatelessWidget {
  const AppButton({
    super.key,
    required this.label,
    this.onPressed,
    this.isLoading,
    this.isDisabled,
  });

  final String label;

  final void Function()? onPressed;

  final bool? isLoading;

  final bool? isDisabled;

  @override
  Widget build(BuildContext context) {
    final tokens = AppTheme.of(context).button;

    return GestureDetector(
      onTap: isDisabled ? null : onPressed,
      child: Container(
        padding: tokens.padding,
        decoration: BoxDecoration(
          color: isDisabled ? tokens.bgColor.withOpacity(0.5) : tokens.bgColor,
          borderRadius: BorderRadius.circular(tokens.radius),
          border: tokens.borderWidth > 0
              ? Border.all(
                  color: tokens.borderColor ?? Colors.black,
                  width: tokens.borderWidth,
                )
              : null,
        ),
        child: isLoading
            ? const SizedBox(
                width: 20,
                height: 20,
                child: CircularProgressIndicator(
                  strokeWidth: 2,
                  valueColor: AlwaysStoppedAnimation<Color>(Colors.white),
                ),
              )
            : Text(
                label,
                style: TextStyle(
                  color: tokens.textColor,
                  fontWeight: FontWeight.bold,
                ),
              ),
      ),
    );
  }
}
