// ============================================
// GENERATED BY SYNTAXIFY v0.1.0
// DO NOT MODIFY - Regenerated on build
// Component: AppButton (Meta-Driven)
// Generated: 2025-12-23T00:26:32.859547
// ============================================

import 'package:flutter/material.dart';
import '../../design_system/design_system.dart';

/// A design-system-aware Button component.
///
/// This widget delegates rendering to [DesignStyle.renderButton].
/// Properties are defined in the corresponding .meta.dart file.
class AppButton extends StatelessWidget {
  const AppButton({
    super.key,
    required this.label,
    this.variant = ButtonVariant.primary,
    this.onPressed,
    this.isLoading = false,
    this.isDisabled = false,
  });

  /// Creates a primary button.
  const AppButton.primary({
    super.key,
    required this.label,
    this.onPressed,
    this.isLoading = false,
    this.isDisabled = false,
  }) : variant = ButtonVariant.primary;

  /// Creates a secondary button.
  const AppButton.secondary({
    super.key,
    required this.label,
    this.onPressed,
    this.isLoading = false,
    this.isDisabled = false,
  }) : variant = ButtonVariant.secondary;

  /// Creates a outlined button.
  const AppButton.outlined({
    super.key,
    required this.label,
    this.onPressed,
    this.isLoading = false,
    this.isDisabled = false,
  }) : variant = ButtonVariant.outlined;

  /// Creates a text button.
  const AppButton.text({
    super.key,
    required this.label,
    this.onPressed,
    this.isLoading = false,
    this.isDisabled = false,
  }) : variant = ButtonVariant.text;

  /// The button label text Maps to: LayoutNode.button(label: ...)
  final String label;

  /// Button variant - references ButtonVariant enum from design_system (primary, secondary, outlined, text) Maps to: LayoutNode.button(variant: ...)
  final ButtonVariant variant;

  /// Callback when button is pressed Maps to: LayoutNode.button(onPressed: ...)
  final VoidCallback? onPressed;

  /// Whether the button shows loading state Maps to: LayoutNode.button(isLoading: ...)
  final bool isLoading;

  /// Whether the button is disabled Maps to: LayoutNode.button(isDisabled: ...)
  final bool isDisabled;

  @override
  Widget build(BuildContext context) {
    return AppTheme.of(context).style.renderButton(
      label: label,
      variant: variant,
      onPressed: onPressed,
      isLoading: isLoading,
      isDisabled: isDisabled,
    );
  }
}
