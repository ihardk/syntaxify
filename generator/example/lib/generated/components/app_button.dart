// ============================================
// GENERATED BY FORGE v0.1.0
// DO NOT MODIFY - Regenerated on build
// Component: Button (Renderer Pattern)
// Generated: 2025-12-17T13:01:49.661268
// ============================================

import 'package:flutter/material.dart';
import '../tokens/design_system.dart';

/// A customizable button component.
///
/// Uses the renderer pattern: WHAT is defined here,
/// HOW is delegated to [DesignStyle.renderButton].
///
/// Usage:
/// ```dart
/// AppButton.primary(label: 'Submit', onPressed: () {})
/// AppButton.outlined(label: 'Cancel', onPressed: () {})
/// ```
class AppButton extends StatelessWidget {
  const AppButton({
    super.key,
    required this.label,
    this.onPressed,
    this.variant = ButtonVariant.primary,
    this.isLoading = false,
    this.isDisabled = false,
  });

  /// Primary button variant (emphasized).
  const AppButton.primary({
    super.key,
    required this.label,
    this.onPressed,
    this.isLoading = false,
    this.isDisabled = false,
  }) : variant = ButtonVariant.primary;

  /// Secondary button variant (less emphasis).
  const AppButton.secondary({
    super.key,
    required this.label,
    this.onPressed,
    this.isLoading = false,
    this.isDisabled = false,
  }) : variant = ButtonVariant.secondary;

  /// Outlined button variant (transparent background).
  const AppButton.outlined({
    super.key,
    required this.label,
    this.onPressed,
    this.isLoading = false,
    this.isDisabled = false,
  }) : variant = ButtonVariant.outlined;

  /// The button label text.
  final String label;

  /// Callback when button is pressed.
  final VoidCallback? onPressed;

  /// The button variant (primary, secondary, outlined).
  final ButtonVariant variant;

  /// Whether the button shows loading state.
  final bool isLoading;

  /// Whether the button is disabled.
  final bool isDisabled;

  @override
  Widget build(BuildContext context) {
    // Delegate to style's renderer (HOW)
    return AppTheme.of(context).style.renderButton(
          label: label,
          variant: variant,
          onPressed: onPressed,
          isLoading: isLoading,
          isDisabled: isDisabled,
        );
  }
}
