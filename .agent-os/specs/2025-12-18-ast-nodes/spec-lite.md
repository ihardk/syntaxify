# Spec Summary (Lite)

Define all AST nodes for Syntaxify: layout nodes (Column, Row, Stack, List, Wrap, Scroll), primitive nodes (Text, Button, TextField, Icon, Image, Toggle, Spacer, Divider), and container nodes (Card, Container). All nodes must use only primitives and semantic strings—no runtime types, callbacks, or framework constructs.
