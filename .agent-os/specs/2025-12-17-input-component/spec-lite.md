# Spec Summary (Lite)

Implement a framework-agnostic `Input` component that generates `TextField` widgets for Material, Cupertino, and Neo styles. Key features include tappable prefix/suffix icons, external controller support for validation, and standard event callbacks (`onChanged`, `onSubmitted`).
