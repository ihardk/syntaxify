# Spec Summary (Lite)

Implement `syntaxify create` commands to scaffold `meta/*.meta.dart` (components), `meta/*.screen.dart` (screens), and `design_system/styles/*_style.dart` (styles). This standardizes file creation and reduces boilerplate for users extending the system.
