# Spec Summary (Lite)

Improve the `ScreenGenerator` subsystem to fully parse P0 screen definitions (including properties like `onPressed` and `hint`) and emit code that utilizes the Syntaxify Design System components (`AppButton`, `AppInput`) instead of raw Material widgets.
