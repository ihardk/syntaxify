# Spec Summary (Lite)

Redesign Syntaxify DX for v0.2.0-beta: Unify variant system where variants are defined in `@SyntaxComponent(variants: [...])` and enums auto-generate. Simplify API to use `App.xxx()` instead of `LayoutNode.xxx()`. Rename enums to `MainAlignment` instead of `SyntaxMainAxisAlignment`. Eliminate import collisions. Target: 60% less boilerplate, single import for screen files.
